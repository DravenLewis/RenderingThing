
- Long Loading Times,
    App Launches Black and unresposnive untill all assets load,
    lets make the UI appear but while the Scene is loading the viewport will be black with
    centered text that says "Please wait as the Scene is Loaded%s", and format it with {"",".","..","..."} where it cycles between them every second, if i > length, i = 0 so it loops.
    also DemoScene is being loaded then the cached scene is being loaded after. 
    can we load the cached scene first, then fallback to demoscene.

Add+
Glass BSDF
Water BSDF

Scripting Component